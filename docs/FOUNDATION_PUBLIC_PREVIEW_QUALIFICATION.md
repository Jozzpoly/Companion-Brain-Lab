# Foundation Public Preview Qualification

Status: **PUBLIC ARTIFACT PASS · OWNER TORTURE PASS · FOUNDATION CLOSED**

Owner-tested exact runtime:

`b217943e027e66993f0010643b2933b01d4b1e6d`

Validation run `34876312905`:

- TypeScript PASS;
- 31/31 test files PASS;
- 137/137 tests PASS;
- production build PASS;
- npm audit: 0 vulnerabilities.

Operational Pages workflow commit on `main`:

`67c37d8145cdbcbf4044db0a6736418d8c9c6258`

Pages run `34876484531`:

- exact runtime checkout PASS;
- exact source validation PASS;
- Pages configure/upload PASS;
- deploy PASS.

Published artifact id:

`10361440336`

Digest:

`sha256:4b3789c28b1dfaf7d6470720808eccc83b2125d1f87363c470c1fcaa6ad79ebe`

Public URL:

`https://jozzpoly.github.io/Companion-Brain-Lab/`

## Real browser evidence

The earlier public candidate `81877d7...` proved the independent catastrophic-fault surface in the real Owner browser and produced valid deliberate fault JSON. It also exposed the reload-loop probe UX failure preserved in `FOUNDATION_OWNER_FAULT_PROBE_EVIDENCE.md`.

The current public runtime makes that probe one-shot and is the runtime used for the ordinary Owner torture gate.

The Owner exercised the normal public runtime for approximately 123 seconds. Review found no spontaneous fault surface, unexplained whole-runtime freeze, permanent companion shutdown, sticky no-safe-velocity state or uncontrolled recovery loop. Intentional stationary periods remained causally legible and movement resumed normally.

Classification:

**PUBLIC ORDINARY-RUNTIME OWNER TORTURE PASS.**

## Supplemental branch-only mechanical evidence

Branch head `7c2d25dead58ca26d3bb195bc1b91911c60d32a5` adds a test-only DIRECT 180-step full-chain survival rehearsal.

Run `34879978993`:

- 32/32 test files PASS;
- 138/138 tests PASS;
- TypeScript/build PASS;
- npm audit: 0 vulnerabilities.

That test does not alter the public browser runtime; it closes the remaining DIRECT survival coverage gap mechanically.

## Evidence boundary

This preview qualification establishes the foundation substrate as rework-ready. It does **not** qualify the legacy eight-slot relationship policy, fixed pace, missing player right-of-way or current NATURAL dynamic-player cooperation as final design.

Canonical verdict:

[`FOUNDATION_FINAL_READINESS.md`](FOUNDATION_FINAL_READINESS.md)
