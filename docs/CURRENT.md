# Companion Brain Lab — CURRENT

Status: **CANONICAL EXECUTION ENTRYPOINT · COMPANION CORE RECOVERY**
Date: **2026-09-20**

Read this first before continuing the project.

This file records current Owner intent and execution authority after the 2026-09 retrospective and repository cleanup. Historical plans, deleted branch names, closed PRs and old handoffs remain evidence, but they do not outrank this file or newer explicit Owner feedback.

---

## 1. What we are building

The target is not a better follower, pathfinder or behavior list.

The project exists to discover how to build a **competent embodied partner present in the same situation as the player**:

- strong cheap local live cognition;
- useful autonomy without babysitting;
- readable initiative;
- player ↔ companion reactions that feel alive because they have real causes;
- combat and preparation for combat;
- fast player direction later, interpreted as intent rather than rigid puppeteering where appropriate;
- one companion first, later a few;
- higher cognition / LLM later where it adds value rather than replacing the live brain.

Experiential north star:

> During unscripted play, the Owner increasingly treats the companion as a teammate: can rely on it without babysitting, can predict enough to coordinate, notices meaningful initiative, and can correct or direct it quickly when desired.

---

## 2. Owner decisions that are CLOSED unless new evidence falsifies them

### Foundation / movement survival

Canonical historical verdict:

> **FOUNDATION PASS · REWORK-READY**

The Owner accepted that the movement/foundation substrate was good enough to stop blocking the next stage.

This does **not** mean locomotion is final or sacred. It means the question:

> “is movement good enough that we may move on?”

must not be reopened merely because later research generated more instrumentation or more confidence.

Reopen it only if a **new concrete failure in richer teammate gameplay** demonstrates that the movement substrate is blocking the next behavior.

### Architecture freedom

Foundation PASS bought a known, observable, survivable substrate. It did not freeze:

- the legacy eight-slot relationship objective;
- current routing;
- S3 velocity lattice/constants;
- NATURAL implementation/constants;
- MotionIntent shape;
- CCC/A1 decomposition;
- any specific selector/weighting scheme.

Preserve earned invariants and falsifiers, not accidental architecture.

---

## 3. What went wrong in the week ending 2026-09-20

The project accumulated a large amount of useful research and evidence while ordinary live authority changed far less.

True ordinary live behavior remained dominated by:

`legacy relationship target → S3/R1 spatial movement → NATURAL temporal realization → World/Rapier`

Meanwhile S5 / CCC / A1 produced substantial research capability, much of it shadow, query-only, pass-through or manually bounded.

The retrospective correctly discovered:

> the lab had become much smarter than the live companion.

The subsequent mistake was to turn that discovery into another movement-only Owner gate.

The public OS-PREP-5 movement review was therefore rejected by the Owner as redundant: it looked approximately like behavior already accepted earlier, because ordinary authority was still approximately the same organism.

**Interpretation:**

- this is **not** evidence that Foundation PASS was invalid;
- this **is** evidence that the project chose the wrong next experimental question;
- Owner attention must not be spent re-approving already closed decisions without new falsifying evidence.

The existing public `/owner/` movement-review surface is historical/operational only. Do not route the Owner back to it as the current test.

---

## 4. Where the project actually is

### Earned substrate

Strong current donors / invariants include:

- World / Rapier owns physical truth and factual outcomes;
- intention, execution and outcome are separate;
- whole-body feasibility matters;
- hard physical legality is distinct from comfort/preference;
- fixed-step embodied execution;
- temporal physical continuity/history;
- post-World causal evidence;
- bounded local recovery;
- trustworthy incident/provenance tooling;
- participant-first judgement plus causal microscope when needed.

### Stage B Slice 1 — OWNER FAIL as a product surface · retain only as bounded donor/evidence

Owner test on 2026-09-20 rejected the Stage B participant build pinned to:

`295452947e5936b79a7e3c40a79f6fdb3f239836`

The Owner's material feedback:

- the build felt like a **regression toward day-one capability**;
- visible product change was experienced as little more than red/green circles added to the old companion;
- the companion still had no meaningful player command/control layer;
- removing the accumulated debug/workbench made the project feel as if prior research had been thrown away;
- presenting this as an Owner gate repeated the same mistake as the earlier rejected movement-review build.

This feedback outranks the earlier machine/browser qualification.

The Stage B mechanism still provides narrow technical evidence:

- live action identity can switch from relationship/regrouping to an external responsibility;
- the existing body/routing/NATURAL chain can realize that objective;
- World can own factual `CONTAINED/BREACHED` outcomes;
- exact causal incident/provenance remains available.

But those facts do **not** make Stage B a useful partner, command system, combat system, or acceptable Owner-facing milestone.

Therefore:

> **Stage B Slice 1 is demoted to a donor/regression fixture. It is not the current product direction and must not be routed back to the Owner as the active test.**

The failure is strategic, not cosmetic. The project narrowed the rich original mission into one autonomous target override and then hid the very causal workbench that the Owner uses to understand and direct the research.

### Debug / Owner Sandbox correction

The project has always treated visual/causal debugging as first-class apparatus. The current repo still contains the R1 Causal Workbench and its accumulated layers; that work was **hidden by the special participant surface, not deleted**.

New invariant:

> **Participant-first interpretation is a sequencing technique, not permission to remove Owner observability.**

Future Owner Sandbox builds must keep the causal workbench and debug layers available in the same runtime, with low-friction collapse/expand if a blind first impression is useful. Do not create a deliberately impoverished Owner surface that hides the project's accumulated state, reasoning, failure evidence or controls.

Still not seriously exercised at product quality:

- fast player direction / command intent;
- autonomy-vs-obedience arbitration;
- tangible combat / action exchange;
- richer threat/focus selection;
- pre-combat readiness;
- explicit disagreement/corrigibility;
- tactical/episodic memory;
- few-companion cooperation;
- live higher cognition.

These are now evaluated against the recovered original mission rather than postponed behind another movement-only gate.

---

## 5. Current strategic objective

### Companion Core Recovery — restore the original research axis

The original 2026-09-13 skeleton remains the strongest statement of the mission:

- strong local live brain;
- one companion first, later a few;
- combat **and preparation for combat**;
- fast low-attention player direction inspired by Mount & Blade's command grammar;
- commands interpreted as intent/constraints where appropriate rather than joystick puppeteering;
- useful autonomy without babysitting;
- explicit study of **autonomy ↔ obedience**;
- first-class visual/causal debugging;
- later higher cognition only when it adds value.

The Foundation PASS explicitly bought permission for aggressive redesign. The project must stop treating the legacy eight-slot follower plus new target sources as the product.

#### Recovery sequence

This sequence is a planning skeleton, not frozen architecture. Reassess after each real result.

**R0 — Restore the Owner research surface**

- keep the accumulated Causal Workbench and world overlays available in the ordinary Owner runtime;
- preserve incident capture/provenance;
- do not make “blind participant” a separate impoverished build;
- make current public action, player directive, autonomy proposal, arbitration result, execution and World outcome inspectable when those concepts exist;
- retain Stage B threat only as optional apparatus/regression evidence.

**R1 — Player Directive seam**

Introduce the smallest explicit player-direction substrate before building a large command tree.

Required separation:

`player directive → interpreted constraints/priorities → local autonomous proposal → arbitration → public action intent → execution → World outcome`

The directive must not directly mutate World truth or become a disguised movement joystick.

Start with a tiny command vocabulary chosen for information value, not completeness. Current leading candidates from the original research frame are variants of:

- follow / stay with me;
- hold here / preserve responsibility for an area;
- protect / prioritize my safety or nearby threats;
- at-will / relax explicit direction.

Exact labels/UI remain open until implementation planning.

**R2 — Autonomy ↔ obedience experiment**

Use one shared-world problem to force real tension between local initiative and player direction.

The important evidence is not “did the NPC obey?” but:

- what did the player ask?
- what did the local brain independently want to do?
- what constraints did the directive create?
- what action won and why?
- did the result preserve player agency?
- when did the companion legitimately deviate, refuse or reconsider?
- could the Owner understand the decision from the workbench?

**R3 — Tangible action/combat apparatus**

Only after the command/autonomy seam is real, replace the abstract threat proxy as needed with the smallest tangible hostile/action exchange that makes assistance, protection, engagement, disengagement and preparation materially meaningful.

Do not jump straight to a full combat architecture.

**R4 — Owner Sandbox**

The next Owner gate must demonstrate a capability the old follower could not plausibly fake:

- the Owner can direct the companion quickly;
- the companion retains bounded local autonomy;
- a shared problem creates an actual command/autonomy tradeoff;
- debug exposes the complete causal chain;
- the experience is not reducible to “yellow circle chases another target”.

Until that exists, keep implementation and machine/browser falsification inside the agent loop.

### Required perceptual criterion

Before asking the Owner for another meaningful test, the new build should be obviously capable of something the old movement-only organism was not.

Examples of acceptable novelty:

- notices a relevant external problem;
- assumes bounded responsibility without a command;
- acts in a way that can materially help or interfere;
- disengages/repositions/regroups for an intelligible reason;
- exposes an explicit current action/intent that corresponds to real world behavior.

If an Owner build still feels like “the same follower with different instrumentation”, do not present it as a culmination.

---

## 6. How to use historical research

Historical work is **donor evidence, not roadmap authority**.

Important donors include:

- S5 continuous relationship field;
- CCC WHERE / PACE / PLAYER FLOW observations;
- A1.1 player-relative objective-space work;
- A1 situation/provenance and same-physics rehearsal;
- temporal embodiment;
- R1 recovery/failure handling;
- player-agency and hard-safety falsifiers.

Rules:

1. transplant only what a current live deficit needs;
2. prefer bounded live-authority experiments over another long shadow-only campaign;
3. do not promote all research wholesale;
4. do not preserve a mechanism merely because many tests exist around it;
5. do not invent a fourth semantic representation while S5/A1.1 remain viable donor candidates;
6. if a research tranche cannot state what ordinary live behavior it changes, it needs a strong reason to exist.

---

## 7. Owner-attention contract

Before any future Owner test, state clearly:

- **what materially changed in ordinary live behavior;**
- **why Owner judgement is needed now;**
- **what exact uncertainty the test resolves;**
- **what the build deliberately does not contain;**
- **what the Owner should and should not expect to feel/see.**

Never present a narrow diagnostic stimulus as if it were the culmination of a large development period.

Owner-confirmed decisions remain closed unless new evidence materially falsifies them.

---

## 8. Repository / evidence discipline

The 2026-09 campaign created excessive Git branch/PR sprawl. That was process debt.

Current repository rule:

- `main` is the canonical working branch by default;
- do not create branch-per-proof, branch-per-document or branch-per-small-experiment;
- use commits, tests, artifacts and documents for provenance;
- create a temporary branch only when isolation genuinely matters;
- normally keep at most one active development branch;
- merge/reject then delete temporary branches promptly;
- closed historical branch tips are indexed in `archive/branch-manifest-2026-09-20.json`;
- old experiment histories remain reachable through Git ancestry even though their branch names are gone.

Do not let preservation cost become architecture authority again.

---

## 9. Validation discipline

Avoid replaying the entire historical research campaign on every ordinary change.

Use layered evidence:

### Fast development gate
- TypeScript;
- relevant unit/contract tests;
- build;
- short runtime smoke where needed.

### Behavior candidate gate
- tests/falsifiers directly covering changed live authority;
- a small set of durable Foundation survival regressions;
- exact provenance / incident support where useful.

### Expensive research campaign
Run old multi-minute PH/Twin/A1 evidence only when the current question genuinely depends on it.

Historical evidence is preserved for retrieval, not automatically imposed on every iteration.

---

## 10. What `kontynuuj` means now

When the Owner says only `kontynuuj`, `dalej`, `ruszaj` or equivalent:

1. recover this current state and the larger teammate objective;
2. inspect live repository truth before writes;
3. choose the highest-value reversible next step toward **real live teammate capability**;
4. do not mechanically execute an obsolete checklist;
5. do not reopen closed Owner decisions without new falsification;
6. prefer implementation / falsification over another planning layer when enough is known;
7. keep Owner attention out of the loop until judgement is genuinely required;
8. after each meaningful result, reassess whether the next step still serves the north star.

---

## 11. Current operational references

Repository default / canonical branch:

`main`

Historical movement-review runtime still used by the existing public `/owner/` Pages surface:

`510405960acf3cfce0a6985fe6f9da88ec95d0c2`

That SHA remains a useful known baseline and provenance anchor. It is **not** the current product target or Owner gate.

Foundation Owner-qualified historical runtime:

`b217943e027e66993f0010643b2933b01d4b1e6d`

CCC-0 historical zero-authority preview:

`857620b758bdaafcbfbab48da06ff89e7fc238cd`

Current Stage B Owner-judgement candidate:

`295452947e5936b79a7e3c40a79f6fdb3f239836`

Rejected Stage B participant surface (historical evidence only; **not current Owner gate**):

`https://jozzpoly.github.io/Companion-Brain-Lab/teammate/?teammate=1`

Historical branch recovery index:

`archive/branch-manifest-2026-09-20.json`

---

## 12. Immediate execution boundary

Do **not** spend the next tranche:

- polishing the old movement-only review;
- rebuilding branch-per-experiment infrastructure;
- resuming A1 merely because it was unfinished;
- cleaning historical code before product direction requires it;
- creating a huge combat architecture;
- designing final commands / multi-companion / LLM systems prematurely.

The Stage B Owner stimulus has **failed**. Do not polish it or ask the Owner to repeat it.

Current execution frontier:

1. restore the full Owner-visible causal/debug surface on the active development runtime;
2. design and implement the minimal **Player Directive** seam on top of the earned Foundation contracts;
3. make autonomous proposal vs player directive vs arbitration outcome explicitly inspectable;
4. use the existing threat loop only as one cheap pressure fixture where useful, not as the product;
5. add the smallest tangible action/combat interaction only when needed to make command/autonomy consequences real;
6. do not request another Owner test until the runtime contains genuine player direction, bounded autonomy and the causal workbench together.

Do **not** reopen Foundation movement unless richer gameplay produces a concrete falsifier.

Do **not** build final command vocabulary, multi-companion logic, LLM cognition or a full combat game yet.

The immediate next implementation question is:

> **What is the smallest player-directive + local-autonomy contract that lets the Owner tell the companion what they want while still observing a real teammate make bounded local decisions?**

That is the current frontier.
