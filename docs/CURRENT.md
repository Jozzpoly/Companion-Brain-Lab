# Companion Brain Lab — CURRENT

Status: **CURRENT EXECUTION ENTRYPOINT**
Date: **2026-09-18**

Read this first when resuming the active Companion work. This file is intentionally small. It points to the current campaign and defines how short continuation prompts should be interpreted.

Historical plans and qualification documents remain valuable evidence, but they do not outrank this file, current Owner intent, or live repository state.

## 1. Active mission

The active mission is **Owner Sandbox Preparation (OS-PREP)**.

We are no longer trying to prove that increasingly elaborate shadow analyses can exist. We are preparing the first serious campaign in which the accumulated movement/cooperation research is confronted with the question that matters:

> Does any of this produce a companion that the Owner can actually experience as a competent, readable teammate in a causal world?

Movement remains the first language of cooperation, not the product goal.

The immediate task is therefore not to widen automatic authority. It is to make the transition from research evidence to real embodied Owner evidence **safe, inspectable, falsifiable and worth the Owner's time**.

Canonical campaign document:

- [OWNER_SANDBOX_PREPARATION_CAMPAIGN.md](OWNER_SANDBOX_PREPARATION_CAMPAIGN.md)

Long-term Owner intent / north-star source:

- [INITIAL_RESEARCH_SKELETON.md](INITIAL_RESEARCH_SKELETON.md)

## 2. Live checkpoint at creation

Branch:

`experiment/a1-opportunity-persistence`

Planning checkpoint:

`dbe6a0e836531b6cc34812c5abd8f1b3bdbf267a`

Commit:

`fix: type and wire P2 Owner controls`

Exact push validation at that SHA:

- `validate` #1287 — **PASS**
- `npm run check` — **PASS**
- full `browser:audit` — **PASS**
- A1.2p1 embodied shadow evidence upload — **PASS**
- A1.2p2 manual DIRECT evidence upload — **PASS**
- relationship semantic provenance / expiry / post-expiry evidence — **PASS**
- `a1-commitment-live` #12 — **PASS**

This SHA is a planning checkpoint, not a permanently frozen runtime.

**Before any exact claim or write, reacquire the live branch head and relevant CI.** If the branch moved, inspect and classify the delta before continuing.

## 3. Current authority truth

Do not collapse these layers:

- **World / Rapier** owns physical truth and factual outcome.
- The legacy/R1 runtime still supplies baseline movement behavior.
- CCC and most A1 machinery remain research/shadow evidence.
- There is **no automatic A1 production selector**.
- A1 DIRECT/TEMPORAL labels outside P2 do not imply a new automatic movement policy.
- **P2 is deliberately authoritative but only by explicit manual one-step authorization.**
- P2 requires a current paused state, current Owner movement evidence, an explicit preview, an exact current H1 frontier proposal, explicit arm, one World step, outcome confirmation, then auto-disarm.
- P2 does not establish future weighting, right-of-way policy, yield policy, horizon policy or repeated authority.

Do not silently widen this authority because a candidate looks obvious.

## 4. What `kontynuuj` means here

When the Owner says only `kontynuuj`, `dalej`, `ruszaj` or an equivalent short continuation:

1. recover the active objective from this file and the campaign document;
2. reacquire volatile live truth: branch head, meaningful CI and any intervening commits;
3. if live state changed, classify the delta before acting;
4. choose the **highest-value still-authorized next step**, not merely the next old checklist item;
5. execute technical/research work that can be done without Owner judgement;
6. falsify the local hypothesis rather than trying to make it pass;
7. verify the result at the strongest economical evidence layer available;
8. re-ground on the larger Owner Sandbox objective after each local result;
9. continue automatically while a useful, reversible, authorized next step remains;
10. update this execution state only when the material state or next boundary actually changes.

A short continuation is **not** permission to invent a new project, silently widen authority, merge to `main`, or promote research evidence into gameplay policy.

## 5. Next-step selection rule

Prefer work in this order:

1. **evidence integrity that can invalidate or devalue an Owner run**;
2. **agent-side falsification of central teammate phenomena**;
3. **the smallest apparatus improvement required to expose those phenomena honestly**;
4. **participant-first / blind interpretation before debug explanation**;
5. **a bounded Owner judgement gate when the remaining uncertainty is genuinely feel, trust, readability or value**;
6. only after repeated desired behavior: reconsider automatic authority or larger architecture.

Do not keep polishing instrumentation once it is strong enough to answer the next product question.

## 6. Active tranche

**OS-PREP-1 — Evidence integrity and session identity**

Current first targets:

- bind P2 preview / arm / application evidence into incident/session capture;
- bind captured evidence to exact build/commit identity;
- ensure capture itself is observational/non-interfering;
- ensure a future Owner run cannot accidentally be attributed to the old public Foundation Pages runtime;
- then build the claim → observable phenomenon map and multi-beat pre-Owner campaign.

The campaign document defines acceptance and downstream phases.

## 7. Owner judgement boundary

Do not ask the Owner to perform routine technical validation that can be automated.

Escalate to the Owner when the remaining question is genuinely experiential or directional, for example:

- does the companion feel cooperative rather than obstructive?
- is the behavior readable without debug labels?
- does initiative preserve agency?
- does a behavior feel like a teammate or an optimizer artifact?
- is a surprising action desirable?

Before that point, prepare the smallest useful build/test and remove avoidable technical burden.

## 8. Explicit anti-goals

Until new evidence changes the campaign:

- no automatic `singleton frontier -> execute`;
- no hidden winner selection;
- no generic right-of-way/yield FSM merely to make behavior look coherent;
- no broad TEMPORAL authority promotion;
- no large P1/browser-forensics detour for aesthetic completeness;
- no transplant of SPC/Feniks architecture into Companion;
- no treating green tests as teammate-quality evidence;
- no treating the old public Pages runtime as the current Owner candidate;
- no forcing the Owner to decode proposal IDs, run terminal commands or act as a routine QA courier;
- no feature expansion into combat/commands/multiple companions before the current embodied cooperation question has produced useful evidence.

## 9. Cross-project donor rule

SPC and Feniks may inform **methodology and pressure**, not override Companion-local truth.

High-value current donors:

- participant-first evidence before debug interpretation;
- World truth vs actor/semantic knowledge vs presentation separation;
- observer non-interference;
- Twin-World / controlled intervention comparison;
- first-divergence / causal slicing;
- material constraints rather than theatrical behavior;
- explicit distinction between physical possibility and higher-level meaning.

Import a mechanism only when a Companion-local problem earns it.

## 10. Stop conditions

Stop automatic continuation and surface the boundary when:

- a real Owner judgement is the missing evidence;
- the next step is destructive, difficult to reverse, or strategically changes the project's mission;
- live evidence materially contradicts the current north star or campaign assumptions;
- the active branch has changed in a way that cannot be safely classified;
- there is no remaining high-value agent-side work before the human edge.

A local PASS is not itself a stop condition.
