# OS-PREP-5 — Revised Movement-Review Candidate Qualification

Status: **TECHNICALLY QUALIFIED · PUBLIC DEPLOYMENT NOT AUTHORIZED**
Date: **2026-09-20**

Canonical retrospective:

- [RETROSPECTIVE_T7_DECISION_DOSSIER.md](RETROSPECTIVE_T7_DECISION_DOSSIER.md)

Preparation contract:

- [OS_PREP_5_REVISED_MOVEMENT_REVIEW_PREPARATION.md](OS_PREP_5_REVISED_MOVEMENT_REVIEW_PREPARATION.md)

Exact qualified candidate:

`510405960acf3cfce0a6985fe6f9da88ec95d0c2`

Source branch at qualification:

`experiment/os-prep5-movement-substrate-owner-review`

Validation:

- GitHub Actions Validate **#1370**;
- run id `35476301593`;
- exact head SHA matched `510405960acf3cfce0a6985fe6f9da88ec95d0c2`;
- conclusion: **SUCCESS / FULL GREEN**;
- completed 2026-09-20T00:00:40Z.

---

## 1. What this candidate is

This is **not** a newly redesigned companion.

It is the already-qualified movement organism wrapped in a stronger experimental boundary so the Owner can review exactly one live stimulus:

- `SPATIAL`;
- `NATURAL`;
- A1 automatic authority `OFF`;
- P2 unavailable;
- 1x time;
- RUNNING;
- existing relationship/routing/spatial/NATURAL/recovery behavior unchanged.

The candidate exists to answer the narrow movement-substrate closure question:

> **Is the current embodied movement/relationship substrate good enough — and sufficiently worth preserving — that Companion Brain Lab should stop treating locomotion as its main research frontier and begin the first true minimum-useful-partner experiment?**

It does not claim broad teammate quality.

---

## 2. Delta from the previously qualified package

Parent participant package:

`437cd11727eba327b99d8d70c80ce031c003a040`

The revised candidate changes:

- participant-mode boundary;
- participant-only control surface;
- research-query sanitization;
- historical workbench-key authority while `owner=1`;
- browser qualification for those contracts;
- retrospective/preparation documentation.

It does **not** change the live gameplay algorithms for:

- relationship selection;
- routing;
- local spatial locomotion;
- hard-vs-comfort handling;
- preferred-velocity refinement;
- NATURAL motion realization;
- progress/recovery;
- World/Rapier.

This separation is independently supported by the full inherited regression suite below.

---

## 3. Participant-mode authority boundary

In `?owner=1`:

### Fixed stimulus

- companion mode = `SPATIAL`;
- actuator = `NATURAL`;
- A1 = `OFF`;
- time scale = `1x`;
- runtime remains running.

### Allowed participant controls

- WASD movement;
- Open;
- Pillar;
- Doorway;
- Head-on;
- Reset;
- Save incident.

### Blocked research controls

Historical keys are ignored:

- `M` — Brain mode;
- `N` — actuator;
- `T` — time scale;
- `P` — pause;
- `O` — single step.

Panel-side research actions are also rejected by an explicit allowlist.

The research panel itself is not disclosable from the Owner surface.

---

## 4. Query-composition falsifier

The revised browser specimen deliberately opened the Owner entrypoint with conflicting apparatus flags:

`?owner=1&a1debug=1&a1p2=1&semanticpush=1&foundationFaultProbe=1`

Runtime installation sanitized this into the participant-safe query.

Artifact evidence confirmed:

- P2 unavailable;
- semantic perturbation not active;
- fault probe not armed;
- research content hidden;
- no research disclosure;
- stimulus remained SPATIAL / NATURAL / A1 OFF / 1x / RUNNING after attempts to use historical research keys.

This is stronger than simply hiding controls.

---

## 5. Candidate-specific browser evidence

Validate #1370:

- revised Owner movement-review specimen — **PASS**;
- evidence artifact upload — **PASS**.

Artifact:

`os-prep5-owner-sandbox-510405960acf3cfce0a6985fe6f9da88ec95d0c2`

Artifact id:

`10594254961`

Direct artifact inspection confirmed:

- schema `companion-brain-lab-os-prep5-movement-review-candidate-v1`;
- exact source SHA = `510405960acf3cfce0a6985fe6f9da88ec95d0c2`;
- build state = `PINNED_SOURCE_SHA`;
- participant rail width ~132 px;
- visible controls = Open / Pillar / Door / Head-on / Reset / Save;
- research disclosure unavailable;
- research content = `display:none`;
- P2 unavailable;
- ignored mutation attempts = M / N / T / P / O;
- incident remained SPATIAL / NATURAL / A1 OFF / 1x / RUNNING;
- recent real Owner +X input preserved in causal evidence;
- scenario switching preserved participant boundary;
- page errors = 0;
- console errors = 0;
- failed requests = 0.

The actual participant screenshot was also inspected and showed the intended clean world + narrow participant rail, without A1/CCC/route/debug overlays or causal terminology.

---

## 6. Full inherited regression qualification

The candidate passed the entire inherited Validate tail, not only the new UI/specimen.

PASS includes:

- `npm run check`;
- aggregate `browser:audit`;
- OS-PREP-1 incident/provenance specimen;
- PH-07 pillar mirror;
- PH-05 doorway settle M1;
- PH-05 doorway reversal;
- PH-04 head-on;
- PH-04 late cross-front;
- PH-01 open turn/reversal;
- PH-01 mirrored turn/reversal;
- PH-03 slow-walk;
- PH-03 sustained slow-walk;
- live semantic provenance replay;
- semantic expiry behavior;
- post-expiry disturbance;
- post-expiry Twin shadow;
- A1.2p1 evidence upload;
- A1.2p2 evidence upload;
- Z4f live class map;
- Z4g stage attribution / canonicalization / upload.

This is important because the revised surface intentionally preserves known behavior/pathologies rather than silently improving them.

---

## 7. Known pathologies deliberately preserved

Qualification does not erase or downgrade:

- PH-02 stale retained semantic policy;
- PH-07 deterministic route-side tie bias;
- PH-01/PH-08 semantic/objective discontinuities that NATURAL motion can visually smooth;
- legacy eight-slot relationship semantics.

The Owner review is intended to establish **their perceptual/product salience**, not hide them before judgement.

---

## 8. Capture boundary

Incident Save remains observational.

The candidate retains the bounded recent causal window already qualified by OS-PREP:

- trace capacity 480 causal frames;
- incident exports the most recent 240 frames;
- roughly ~4 seconds at a 60 Hz World cadence.

Do not widen into continuous recording without Owner evidence that the bounded bookmark window is insufficient.

---

## 9. Timing / observer boundary

CCC/A1 research observation remains present where historically present, but does not own ordinary movement output.

Existing browser evidence separately qualified timing non-interference strongly enough for this Owner experiment:

- no >250 ms / >1 s catastrophic stalls in qualified browser timing evidence;
- A1.1 synchronous observer timing was separately characterized without movement-output interference.

This is not a claim about subjective smoothness on the Owner machine.

That uncertainty is deliberately left to the real Owner run.

---

## 10. Qualification conclusion

Exact SHA `510405960acf3cfce0a6985fe6f9da88ec95d0c2` is:

> **TECHNICALLY QUALIFIED AS THE REVISED OWNER MOVEMENT-SUBSTRATE REVIEW STIMULUS.**

This means:

- preparation is complete;
- gameplay identity is bounded;
- participant contamination routes are defended;
- provenance is reconstructable;
- full inherited regression is green.

It does **not** mean:

- teammate quality PASS;
- movement-substrate Owner acceptance;
- broad OS-PREP completion;
- public deployment authorization;
- permission to promote A1/S5/commands/combat.

---

## 11. Next boundary

The next safe technical action is a **non-public Pages-like dry-run** pinned to exact `510405...`, updating the old provisional operations rehearsal to the revised participant contract.

Only after that dry-run passes should the project reach a public deployment decision.

Do not repoint real GitHub Pages automatically.
