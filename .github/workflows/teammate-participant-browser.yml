name: teammate-participant-browser

on:
  push:
    branches:
      - main
    paths:
      - "src/app/owner-review-mode.ts"
      - "src/app/r1-lab-scene.ts"
      - "src/app/shared-danger-apparatus-hud.ts"
      - "src/debug/causal-panel.ts"
      - "src/brain/situated-responsibility.ts"
      - "src/brain/s3-material-contribution.ts"
      - "src/brain/s4-correction.ts"
      - "src/world/shared-danger-contract.ts"
      - "src/world/world.ts"
      - "index.html"
      - "scripts/teammate-participant-browser-smoke.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/teammate-participant-browser.yml"
  workflow_dispatch:

concurrency:
  group: teammate-participant-browser-${{ github.ref }}
  cancel-in-progress: true

jobs:
  browser:
    runs-on: ubuntu-latest
    env:
      VITE_SOURCE_SHA: ${{ github.sha }}
      GITHUB_SHA: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Install dependencies
        run: |
          if npm install; then
            exit 0
          fi
          latest_log="$(ls -t "$HOME"/.npm/_logs/*-debug-0.log 2>/dev/null | head -n 1 || true)"
          if [ -z "$latest_log" ] || ! grep -Fq "Cannot read properties of null (reading 'edgesOut')" "$latest_log"; then
            if [ -n "$latest_log" ]; then cat "$latest_log"; fi
            exit 1
          fi
          rm -rf node_modules
          npm install --legacy-peer-deps
      - name: Causal regression subset
        run: npx vitest run src/brain/situated-responsibility.test.ts src/brain/s3-material-contribution.test.ts src/brain/s4-correction.test.ts
      - name: Build ordinary runtime
        run: npm run build
      - name: Participant teammate browser gate
        run: npm run browser:teammate-participant
      - name: Upload participant evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: teammate-participant-browser-${{ github.sha }}
          path: artifacts/teammate-participant-browser/
          if-no-files-found: warn
          retention-days: 14
