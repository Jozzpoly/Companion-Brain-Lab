{
  "name": "companion-brain-lab",
  "version": "0.0.0-s1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "check": "tsc -p tsconfig.json --noEmit && vitest run && vite build"
  },
  "dependencies": {
    "@dimforge/rapier2d-deterministic-compat": "0.20.0",
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
